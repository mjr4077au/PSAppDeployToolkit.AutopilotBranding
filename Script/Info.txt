Install-DesiredStateManagement.ps1

Desired State Management by The Missing Link is an automated solution to pre-configure workstations via schema-validated XML runlist to automate app removals, first-time logon setup for users, and more.

The Missing Link
2.7

When config/content is hosted:
%windir%\sysnative\WindowsPowerShell\v1.0\powershell.exe -ExecutionPolicy Bypass -NoProfile -File Install-DesiredStateManagement.ps1 -Install
%windir%\sysnative\WindowsPowerShell\v1.0\powershell.exe -ExecutionPolicy Bypass -NoProfile -File Install-DesiredStateManagement.ps1 -Remove

When using wrapper to package content:
%windir%\sysnative\cmd.exe /c "copy /V /Y Install-DesiredStateManagement.ps1 %windir%\system32"
%windir%\sysnative\cmd.exe /c "del /F /Q %windir%\system32\Install-DesiredStateManagement.ps1"

Determine behaviour based on return codes.